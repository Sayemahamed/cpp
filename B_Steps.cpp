#include<bits/stdc++.h>
using namespace std;
long long xAxis,yAxis,xCurrent,yCurrent;
int main()
{
    cin >> xAxis >> yAxis >> xCurrent >> yCurrent;
    long long test,ans = 0;
    cin >>  test;
    while (test--)
    {
        long long x,y;
        cin >> x >>y;
        if(x>=0 and y>=0)
        ans += min(abs()
    }
}