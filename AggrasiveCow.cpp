#include<bits/stdc++.h>
using namespace std;
const long long limit=1e7;
long long stalls,cow,stallPositions[limit];
int main()
{
    long long test;
    cin >> test;
    while (test--) 
    {
        cin >>stalls>> cow;
        
    }
}